# Runtime dependency constraints for reproducible Raspberry Pi installs.
#
# install-runtime-deps.sh installs the project with:
#   pip install -c requirements/constraints.txt --no-build-isolation -e /opt/InstantLinkBridge
#
# Keep this file in sync with pyproject.toml when changing runtime dependencies.
# The deployment manifest records this file's SHA-256 so a Pi can be tied back
# to the exact dependency constraint set used for installation.

# Packaging/build tooling used by install-runtime-deps.sh.
pip==26.0.1
setuptools==82.0.1
wheel==0.47.0
hatchling==1.27.0
packaging==26.2
pathspec==1.1.1
pluggy==1.6.0
trove-classifiers==2026.5.20.19
editables==0.6

# Direct runtime dependencies from pyproject.toml.
aiohttp==3.13.5
bleak==1.1.1
dbus-fast==2.45.1
gpiozero==2.0.1
lgpio==0.2.2.0
luma.lcd==2.13.0
Pillow==11.3.0
pillow-heif==1.3.0
pyftpdlib==2.2.0
rawpy==0.27.0
sdnotify==0.3.2

# Runtime transitive pins for the Pi deployment environment.
aiohappyeyeballs==2.6.1
aiosignal==1.4.0
attrs==26.1.0
cbor2==5.9.0
colorzero==2.0
frozenlist==1.8.0
idna==3.15
luma.core==2.5.3
multidict==6.7.1
numpy==2.4.6
propcache==0.4.1
pyasynchat==1.0.5 ; python_version >= "3.12"
pyasyncore==1.0.5 ; python_version >= "3.12"
rpi-lgpio==0.6
smbus2==0.6.1
spidev==3.8
typing-extensions==4.15.0 ; python_version < "3.13"
yarl==1.22.0
